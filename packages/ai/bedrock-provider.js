/**
 * 文件职责：作为包根目录的兼容入口，转发 Amazon Bedrock 提供方的构建产物。
 * 技术维度：使用 ECMAScript 模块的通配符导出，将运行时实现从 dist 目录重新公开。
 * 产品维度：用户可从稳定的包路径导入 Bedrock 能力，无需了解内部构建目录。
 * 逻辑维度：模块加载时直接转交所有命名导出，不创建状态也不执行额外处理。
 * 关键边界：运行前必须存在 dist/bedrock-provider.js；本入口不会处理缺失文件或版本不匹配。
 * 新手阅读建议：把它视为“门牌号”文件，真实实现应继续阅读 dist 对应源代码的生成来源。
 */
export * from "./dist/bedrock-provider.js";
